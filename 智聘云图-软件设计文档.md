# 智聘云图 — 基于文档智能解析的人岗匹配平台 软件设计文档

> **文档版本**: v2.0 (完整版)
> **编制日期**: 2026-07-06
> **开发环境**: Windows 10/11 (开发与联调)
> **部署环境**: 银河麒麟高级服务器 V11 + LoongArch 架构 (生产部署)
> **AI 模型**: 豆包 doubao-seed-2-0-lite-260428 (火山引擎 ARK API)
> **目标定位**: 省级人才服务中心 10万+ 简历 / 5000+ 企业 智能匹配平台

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统总体架构](#2-系统总体架构)
3. [技术选型](#3-技术选型)
4. [功能模块设计](#4-功能模块设计)
5. [数据库设计](#5-数据库设计)
6. [API 接口设计](#6-api-接口设计)
7. [前端完整页面设计](#7-前端完整页面设计)
8. [AI 与知识图谱设计](#8-ai-与知识图谱设计)
9. [核心业务时序图](#9-核心业务时序图)
10. [安全与性能设计](#10-安全与性能设计)
11. [部署与运维](#11-部署与运维)
12. [实施计划与里程碑](#12-实施计划与里程碑)

---

## 1. 项目概述

### 1.1 项目背景

某省级人才服务中心管理 **10万+ 份人才简历**, 服务 **5000+ 家企业**。传统招聘模式存在三大痛点:

| 痛点 | 现状描述 | 业务影响 |
|------|---------|---------|
| 简历筛选耗时耗力 | HR 人工阅读数千份简历 | 单岗位平均筛选周期 5-7 天 |
| 岗位要求描述非结构化 | Word/Doc 自由文本格式 | 关键信息抽取依赖经验 |
| 人岗匹配依赖主观经验 | 缺乏量化契合度模型 | 匹配精准度 < 40% |

### 1.2 项目目标

构建基于 **文档智能解析 + 知识图谱 + 豆包大模型** 的 B/S 架构平台, 实现:

- 简历/职位 DOC、PDF 文档一键上传, AI 自动结构化入库
- 个人能力图谱与职位能力图谱可视化构建
- 双向智能推荐(求职者 → 岗位 / 企业 → 候选人)
- 多维度匹配评分(技能、经历、城市、意向等)
- 综合后台管理与运营监控

### 1.3 核心价值指标

| 指标 | 目标值 | 度量方式 |
|------|--------|---------|
| 文档解析准确率 | ≥ 90% | 关键字段 F1-Score |
| 匹配推荐 Top10 命中率 | ≥ 75% | 人工标注样本集 |
| 单简历解析耗时 | ≤ 10s | P95 延迟 |
| 平台并发支持 | ≥ 200 QPS | 压测峰值 |
| 知识图谱节点规模 | ≥ 50万 | 节点总数 |

### 1.4 用户角色总览

| 角色 | 标识 | 核心场景 |
|------|------|---------|
| 个人用户 (Seeker) | `ROLE_SEEKER` | 上传简历、查看能力图谱、接收职位推荐、查看匹配度 |
| 企业用户 (Employer) | `ROLE_EMPLOYER` | 上传职位文档、查看职位图谱、接收候选人推荐、查看匹配分析 |
| 平台管理员 (Admin) | `ROLE_ADMIN` | 用户管理、数据维护、图谱管理、监控统计、系统配置 |

---

## 2. 系统总体架构

### 2.1 架构概览

智聘云图采用 **前后端分离 + 微服务化 + AI 中台** 的分层架构:

```
┌─────────────────────────────────────────────────────────────┐
│                     接入层 (Access Layer)                     │
│   PC Web (Vue3)  │  Nginx 反向代理  │  HTTPS/WSS            │
└──────────────────────────────┬──────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────┐
│                  应用服务层 (Application Layer)               │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌──────────┐ │
│  │ User-Svc   │ │ Doc-Svc    │ │ Graph-Svc  │ │ Match-Svc│ │
│  │ 用户服务    │ │ 文档解析服务│ │ 图谱服务    │ │ 匹配服务  │ │
│  └────────────┘ └────────────┘ └────────────┘ └──────────┘ │
│  ┌────────────┐ ┌────────────┐ ┌────────────────────────┐  │
│  │ Admin-Svc  │ │ Notify-Svc │ │   API Gateway (FastAPI)│  │
│  │ 后台服务    │ │ 通知服务    │ │   路由/鉴权/限流       │  │
│  └────────────┘ └────────────┘ └────────────────────────┘  │
└──────────────────────────────┬──────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────┐
│                   AI 中台层 (AI Middle-End)                  │
│  ┌──────────────┐ ┌──────────────┐ ┌────────────────────┐  │
│  │ ARK Gateway  │ │ Parser Engine│ │ Graph Builder      │  │
│  │ 豆包模型网关  │ │ 文档解析引擎  │ │ 图谱构建器          │  │
│  │ (火山引擎)   │ │ (OCR+NLU)   │ │ (Neo4j + 自研)     │  │
│  └──────────────┘ └──────────────┘ └────────────────────┘  │
│  ┌──────────────┐ ┌──────────────┐ ┌────────────────────┐  │
│  │ Skill Tagger │ │ Match Engine │ │ Embedding Service  │  │
│  │ 技能标签器    │ │ 匹配引擎      │ │ 向量化服务          │  │
│  └──────────────┘ └──────────────┘ └────────────────────┘  │
└──────────────────────────────┬──────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────┐
│                   数据存储层 (Data Layer)                     │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌──────────┐ │
│  │ 达梦 DM8   │ │ Neo4j      │ │ Redis      │ │ MinIO    │ │
│  │ 关系型DB   │ │ 图数据库   │ │ 缓存/会话   │ │ 对象存储 │ │
│  └────────────┘ └────────────┘ └────────────┘ └──────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 双环境策略 (Windows 开发 + LoongArch 部署)

> **关键约束**: 赛题要求最终部署于 **LoongArch 架构 + 银河麒麟高级服务器 V11/V10**, 不满足视为 0 分。开发阶段在 Windows 环境进行, 完成后迁移至 LoongArch 部署。

```
┌──────────────────────────────────────┐    ┌──────────────────────────────────────┐
│      Windows 开发环境 (当前阶段)      │    │   LoongArch + 麒麟 部署环境 (生产)    │
│                                      │    │                                      │
│  • Node 20 + npm (前端构建)          │    │  • Docker (loong64) 容器化部署       │
│  • Python 3.11 + venv (后端开发)     │ ──▶│  • 达梦 DM8 (loong64) 替换 SQLite    │
│  • SQLite / MySQL (本地开发库)       │    │  • Neo4j (loong64) 图数据库          │
│  • Redis Windows 版 (本地缓存)       │    │  • Nginx (loong64) 反向代理          │
│  • MinIO Windows 版 (本地存储)       │    │  • 全组件 loong64 镜像               │
│  • 豆包 ARK API (云端调用, 无需本地) │    │  • 豆包 ARK API (云端调用)           │
└──────────────────────────────────────┘    └──────────────────────────────────────┘
```

**环境差异处理**:
- 配置项通过 `.env` 环境变量隔离, 开发用 `.env.dev`, 部署用 `.env.prod`
- 数据库驱动通过 SQLAlchemy URI 切换 (开发 `sqlite:///./zhipin.db`, 部署 `dm+dmPython://...`)
- 文件存储抽象层: 开发用本地磁盘, 部署用 MinIO
- AI 调用: 开发与部署均走云端豆包 ARK API, 无需本地模型

### 2.3 分层职责说明

| 层级 | 职责 | 关键组件 |
|------|------|---------|
| 接入层 | 流量入口、TLS 终止、静态资源分发 | Nginx 1.24 |
| 应用服务层 | 业务逻辑编排、RESTful API 暴露 | FastAPI 0.110 + Uvicorn |
| AI 中台层 | 豆包模型调度、文档解析、图谱构建、匹配计算 | ARK SDK + 自研引擎 |
| 数据存储层 | 持久化、缓存、对象存储 | 达梦 DM8 + Neo4j + Redis + MinIO |

---

## 3. 技术选型

### 3.1 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue 3 | 3.4.x | 渐进式框架, Composition API |
| Vite | 5.x | 构建工具, HMR 热更新 |
| TypeScript | 5.4 | 类型安全 |
| Element Plus | 2.7 | 企业级 UI 组件库 |
| Vue Router | 4.x | 路由管理 |
| Pinia | 2.1 | 状态管理 |
| ECharts | 5.5 | 数据可视化图表 |
| AntV G6 | 5.x | 知识图谱关系图可视化 |
| Axios | 1.6 | HTTP 请求库 |
| Tailwind CSS | 3.4 | 原子化 CSS, 自定义主题 |
| VueUse | 10.x | 组合式工具函数 |

### 3.2 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.11 | 主语言 |
| FastAPI | 0.110 | 异步 Web 框架, OpenAPI 自动生成 |
| Uvicorn | 0.27 | ASGI 服务器 |
| SQLAlchemy | 2.0 | ORM (兼容达梦 DM8 驱动) |
| Celery | 5.3 | 异步任务队列 (文档解析、匹配计算) |
| RabbitMQ | 3.13 | 消息中间件 (开发环境可用 Redis broker) |
| python-docx | 1.1 | DOC 文档解析 |
| pdfplumber | 0.10 | PDF 文档解析 |
| PaddleOCR | 2.7 | 扫描件 OCR 识别 |
| openai (SDK) | 1.x | 调用豆包 ARK API (兼容 OpenAI 协议) |
| pydantic | 2.6 | 数据校验 |
| python-jose | 3.3 | JWT 鉴权 |
| passlib[bcrypt] | 1.7 | 密码哈希 |
| python-multipart | 0.0.6 | 文件上传 |

### 3.3 数据存储

| 组件 | 开发环境 (Windows) | 部署环境 (LoongArch) | 用途 |
|------|-------------------|---------------------|------|
| 关系型 DB | SQLite / MySQL 8 | 达梦 DM8 8.1.2.128 | 主存储 (国产化合规) |
| 图数据库 | Neo4j 5.18 (Windows) | Neo4j 5.18 (loong64) | 知识图谱 |
| 缓存 | Redis 7.2 (Windows) | Redis 7.2 (loong64) | 缓存/会话/限流 |
| 对象存储 | 本地磁盘 / MinIO | MinIO (loong64) | 文档原文件存储 |

### 3.4 AI / LLM 技术

| 能力 | 实现方案 |
|------|---------|
| **对话/解析大模型** | 豆包 `doubao-seed-2-0-lite-260428` (火山引擎 ARK API) |
| **API 协议** | 兼容 OpenAI SDK, base_url=`https://ark.cn-beijing.volces.com/api/v3` |
| **Embedding 向量化** | 豆包 `doubao-embedding-text-240915` (ARK API) |
| 技能实体识别 | 豆包模型 Prompt + 规则后处理 + 技能词典 |
| 图谱构建 | 豆包模型三元组抽取 (头实体-关系-尾实体) |
| 匹配计算 | Embedding 余弦相似度 + 规则加权 + 豆包模型重排序 |
| 匹配依据生成 | 豆包模型生成自然语言解释 |

### 3.5 基础设施

| 组件 | 用途 |
|------|------|
| Docker 24.0 | 容器化 (部署阶段) |
| Docker Compose | 单机编排 |
| Nginx 1.24 | 反向代理、静态资源、负载均衡 |
| Prometheus + Grafana | 监控告警 |
| Loki | 日志聚合 |

---

## 4. 功能模块设计

### 4.1 用户管理模块 (User-Svc)

#### 4.1.1 角色定义

| 角色 | 标识 | 核心权限 |
|------|------|---------|
| 个人用户 | `ROLE_SEEKER` | 上传简历、查看职位推荐、查看能力图谱 |
| 企业用户 | `ROLE_EMPLOYER` | 上传职位、查看候选人推荐、查看职位图谱 |
| 平台管理员 | `ROLE_ADMIN` | 用户管理、数据维护、监控统计 |

#### 4.1.2 注册流程

```
注册流程:
  选择角色(个人/企业)
    → 填写账号(手机号/邮箱) + 密码 + 确认密码
    → 填写角色专属信息
       - 个人: 昵称、姓名、性别
       - 企业: 企业名称、统一社会信用代码、联系人
    → 勾选用户协议
    → 后端校验(账号唯一性 + 密码强度)
    → 密码 BCrypt 加密存储
    → 生成用户记录
    → 签发 JWT (access_token 2h + refresh_token 7d)
    → 跳转对应工作台
```

#### 4.1.3 登录流程

```
登录流程:
  输入账号(手机号/邮箱/用户名) + 密码
    → 后端校验账号存在性 + 密码匹配
    → 校验用户状态(status=1 启用)
    → Redis 记录登录态 (key=user:session:{user_id}, TTL=7d)
    → 返回 JWT + 用户画像摘要(角色、昵称、头像)
    → 前端按角色跳转:
       - ROLE_SEEKER  → /seeker/dashboard
       - ROLE_EMPLOYER → /employer/dashboard
       - ROLE_ADMIN    → /admin/dashboard
```

#### 4.1.4 核心接口

```
POST   /api/v1/auth/register        注册
POST   /api/v1/auth/login            登录
POST   /api/v1/auth/refresh          刷新 Token
POST   /api/v1/auth/logout           退出
GET    /api/v1/users/profile         获取当前用户信息
PUT    /api/v1/users/profile         更新用户信息
PUT    /api/v1/users/password        修改密码
POST   /api/v1/users/avatar          上传头像
```

---

### 4.2 文档上传与智能解析模块 (Doc-Svc)

#### 4.2.1 模块架构

```
┌─────────────┐     ┌──────────────┐     ┌─────────────────┐
│  文件上传    │ ──▶ │  文件预处理   │ ──▶ │  解析任务入队    │
│  DOC/PDF    │     │  格式校验     │     │  Celery Worker  │
└─────────────┘     │  Hash 去重   │     └────────┬────────┘
                    └──────────────┘              │
                                                   ▼
┌─────────────┐     ┌──────────────┐     ┌─────────────────┐
│  结构化存储  │ ◀── │  豆包模型解析 │ ◀── │  内容提取       │
│  达梦 DM8   │     │  ARK API     │     │  OCR + 文本抽取 │
└─────────────┘     └──────────────┘     └─────────────────┘
        │
        ▼
┌─────────────┐
│  图谱构建    │  同步构建知识图谱节点与关系
│  Neo4j      │
└─────────────┘
```

#### 4.2.2 解析流程

1. **上传阶段**: 文件大小限制 20MB, MIME 校验 (`.doc/.docx/.pdf`), 上传至 MinIO/本地磁盘
2. **预处理**: 文件 Hash 去重, 记录文档元数据
3. **内容提取**:
   - DOC/DOCX: `python-docx` 提取段落、表格、列表
   - PDF (文本型): `pdfplumber` 提取文本与布局
   - PDF (扫描型): `PaddleOCR` 识别 + 版面分析
4. **豆包模型智能解析**: 调用 `doubao-seed-2-0-lite-260428`, 按预定义 JSON Schema 输出结构化字段
5. **后处理**: 字段校验、技能词典归一化、实体去重
6. **结构化存储**: 写入数据库, 同步构建知识图谱节点
7. **向量化**: 调用豆包 Embedding 模型生成向量, 存入 `embedding` 字段

#### 4.2.3 简历解析 Schema

```json
{
  "basic_info": {
    "name": "string",
    "gender": "enum[male,female]",
    "age": "int",
    "phone": "string",
    "email": "string",
    "current_city": "string",
    "intention_city": ["string"],
    "education": "enum[博士,硕士,本科,大专,其他]",
    "school": "string",
    "major": "string",
    "work_years": "int",
    "expected_salary": {"min": "int", "max": "int"}
  },
  "skills": [
    {"name": "string", "level": "enum[精通,熟练,掌握,了解]", "category": "string"}
  ],
  "work_experience": [
    {
      "company": "string",
      "position": "string",
      "start_date": "YYYY-MM",
      "end_date": "YYYY-MM",
      "description": "string",
      "achievements": ["string"]
    }
  ],
  "project_experience": [
    {
      "name": "string",
      "role": "string",
      "duration": "string",
      "description": "string",
      "tech_stack": ["string"]
    }
  ],
  "certifications": ["string"],
  "self_evaluation": "string"
}
```

#### 4.2.4 职位解析 Schema

```json
{
  "job_basic": {
    "title": "string",
    "company": "string",
    "department": "string",
    "job_type": "enum[全职,兼职,实习]",
    "salary_range": {"min": "int", "max": "int", "unit": "enum[月,年]"},
    "work_city": "string",
    "experience_required": "string",
    "education_required": "string",
    "headcount": "int"
  },
  "responsibilities": ["string"],
  "requirements": [
    {"type": "enum[必须,优先]", "skill": "string", "level": "string"}
  ],
  "benefits": ["string"],
  "keywords": ["string"]
}
```

#### 4.2.5 核心接口

```
POST   /api/v1/documents/upload          上传文档 (multipart/form-data)
GET    /api/v1/documents/{id}/status     查询解析状态
GET    /api/v1/documents/{id}/result     获取解析结果
GET    /api/v1/resumes/me                获取我的简历列表
GET    /api/v1/resumes/{id}              获取简历详情
DELETE /api/v1/resumes/{id}              删除简历
POST   /api/v1/jobs                       企业发布职位(可基于解析结果编辑)
GET    /api/v1/jobs/me                    获取企业职位列表
GET    /api/v1/jobs                       职位广场(搜索)
GET    /api/v1/jobs/{id}                  职位详情
```

---

### 4.3 能力图谱构建与可视化模块 (Graph-Svc)

#### 4.3.1 图谱 Schema 设计

**个人能力图谱 (Resume Capability Graph)**:

```
节点类型 (Node Labels):
  - Person          个人
  - Skill           技能 (含 category 分类)
  - Experience      工作经历
  - Project         项目经历
  - Education       教育背景
  - Certification   证书

关系类型 (Relationship Types):
  - HAS_SKILL       (Person)-[level: 精通/熟练, weight: 0-1]->(Skill)
  - WORKED_AT       (Person)-[period, position]->(Experience)
  - PARTICIPATED    (Person)-[role]->(Project)
  - GRADUATED_FROM  (Person)-[major, degree]->(Education)
  - HOLDS_CERT      (Person)->(Certification)
  - SKILL_RELATED   (Skill)-[similarity: 0-1]->(Skill)   # 技能关联
```

**职位能力图谱 (Job Capability Graph)**:

```
节点类型:
  - Job             职位
  - Skill           技能要求
  - Responsibility  职责
  - Benefit         福利
  - City            工作城市

关系类型:
  - REQUIRES_SKILL  (Job)-[level: 必须/优先, weight]->(Skill)
  - HAS_RESP        (Job)->(Responsibility)
  - OFFERS_BENEFIT  (Job)->(Benefit)
  - LOCATED_IN      (Job)->(City)
```

#### 4.3.2 图谱构建流程

```
1. 结构化数据 → 三元组抽取 (豆包模型辅助)
2. 技能归一化 (技能词典 + 同义词林)
3. 节点去重 (基于 Embedding 相似度 > 0.92 合并)
4. 关系权重计算 (技能等级映射: 精通=1.0, 熟练=0.8, 掌握=0.6, 了解=0.4)
5. 写入 Neo4j (Cypher MERGE 语句)
6. 生成可视化布局数据 (force-directed 力导引布局)
```

#### 4.3.3 核心接口

```
GET    /api/v1/graph/resume/{resume_id}      获取个人能力图谱数据
GET    /api/v1/graph/job/{job_id}            获取职位能力图谱数据
GET    /api/v1/graph/match/{resume_id}/{job_id}  获取匹配对比图谱
```

---

### 4.4 智能双向匹配推荐模块 (Match-Svc)

#### 4.4.1 匹配算法架构

```
┌─────────────────────────────────────────────────────────┐
│                  匹配引擎 (Match Engine)                  │
│                                                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │  召回阶段    │→│  粗排阶段    │→│  精排阶段    │    │
│  │  Recall     │  │  Co-Rank    │  │  Fine-Rank  │    │
│  └─────────────┘  └─────────────┘  └─────────────┘    │
│   │                │                 │                 │
│   ▼                ▼                 ▼                 │
│  Embedding        规则加权           豆包模型重排序     │
│  向量召回          多维度评分         语义匹配校验       │
│  (Top 200)        (Top 50)          (Top 10)          │
└─────────────────────────────────────────────────────────┘
```

#### 4.4.2 多维度评分模型

**匹配总分 = Σ (维度得分 × 权重) × 100**

| 维度 | 权重 | 计算方式 |
|------|------|---------|
| 技能匹配度 | 0.35 | 必须技能命中 × 1.0 + 优先技能命中 × 0.6 + 等级匹配系数 |
| 经验匹配度 | 0.20 | 工作年限满足度 + 相关行业经验相似度 (Embedding) |
| 学历匹配度 | 0.10 | 学历等级 ≥ 要求等级 |
| 城市匹配度 | 0.15 | 居住城市/意向城市与工作城市匹配 |
| 项目匹配度 | 0.10 | 项目技术栈与职位要求重合度 |
| 薪资匹配度 | 0.05 | 期望薪资与职位薪资区间匹配 |
| 语义匹配度 | 0.05 | 简历自评与职位描述 Embedding 余弦相似度 |

**城市匹配 (多维度加分项)**:
- 意向城市完全匹配 → 1.0 分
- 居住城市匹配 (同城) → 0.8 分
- 同省不同市 → 0.5 分
- 其他 → 0.3 分

#### 4.4.3 双向推荐

| 场景 | 输入 | 输出 |
|------|------|------|
| 求职者 → 推荐职位 | resume_id | Top 10 职位 + 匹配分 + 匹配依据 |
| 企业 → 推荐候选人 | job_id | Top 10 候选人 + 匹配分 + 匹配依据 |

**匹配依据生成**: 调用豆包模型生成自然语言解释, 例如:
> "候选人张三与'高级Python开发工程师'匹配度 87.5 分。命中必须技能: Python(精通)、FastAPI(熟练); 工作年限 6 年 (要求 5 年); 意向城市杭州与工作城市一致; 项目经验中含 2 个微服务架构项目。"

#### 4.4.4 核心接口

```
POST   /api/v1/match/recommend-jobs          为求职者推荐职位
POST   /api/v1/match/recommend-candidates    为企业推荐候选人
GET    /api/v1/match/detail                  获取匹配详情(含依据)
GET    /api/v1/match/graph/{resume_id}/{job_id}  匹配对比图谱
```

---

### 4.5 平台管理与监控后台模块 (Admin-Svc)

#### 4.5.1 功能矩阵

| 子模块 | 功能点 |
|--------|--------|
| 用户管理 | 用户列表、审核、封禁、角色变更、批量导入 |
| 简历管理 | 简历列表、解析状态、重新解析、删除 |
| 职位管理 | 职位列表、审核、下架、统计 |
| 图谱管理 | 节点查询、关系维护、技能词典维护 |
| 匹配管理 | 匹配记录查询、算法参数调优 |
| 监控统计 | 注册量、解析量、匹配量、活跃度看板 |
| 系统配置 | LLM 配置、阈值配置、字典维护 |
| 操作日志 | 管理员操作审计日志 |

#### 4.5.2 核心接口

```
GET    /api/v1/admin/users                   用户列表(分页/筛选)
PUT    /api/v1/admin/users/{id}/status       用户启用/禁用
GET    /api/v1/admin/resumes                 简历列表
POST   /api/v1/admin/resumes/{id}/reparse    重新解析
GET    /api/v1/admin/dashboard/stats         看板统计数据
GET    /api/v1/admin/logs/operations         操作日志
```

---

## 5. 数据库设计

### 5.1 ER 关系概览

```
sys_user 1───∞ resume
sys_user 1───∞ job
resume 1───∞ resume_skill
job 1───∞ job_requirement
resume ∞───∞ job (match_record)
sys_user 1───∞ admin_log
```

### 5.2 表结构设计

> **注**: 开发环境用 SQLite/MySQL, 部署环境用达梦 DM8。DDL 兼容 MySQL 语法。

#### 5.2.1 用户表 `sys_user`

```sql
CREATE TABLE sys_user (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    username        VARCHAR(64)  NOT NULL UNIQUE,
    password_hash   VARCHAR(128) NOT NULL,        -- BCrypt
    role            VARCHAR(16)  NOT NULL,        -- ROLE_SEEKER / ROLE_EMPLOYER / ROLE_ADMIN
    phone           VARCHAR(20)  UNIQUE,
    email           VARCHAR(128) UNIQUE,
    nickname        VARCHAR(64),
    avatar_url      VARCHAR(256),
    -- 企业用户字段
    company_name    VARCHAR(128),
    credit_code     VARCHAR(32),                 -- 统一社会信用代码
    contact_person  VARCHAR(64),
    -- 个人用户字段
    real_name       VARCHAR(64),
    gender          VARCHAR(8),
    status          TINYINT      DEFAULT 1,       -- 0=禁用 1=启用
    last_login_at   TIMESTAMP,
    created_at      TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
    updated_at      TIMESTAMP    DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_user_role ON sys_user(role);
CREATE INDEX idx_user_status ON sys_user(status);
```

#### 5.2.2 简历表 `resume`

```sql
CREATE TABLE resume (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id         BIGINT NOT NULL,
    doc_url         VARCHAR(256) NOT NULL,
    doc_hash        VARCHAR(64),
    parse_status    TINYINT DEFAULT 0,            -- 0=待解析 1=解析中 2=成功 3=失败
    parse_error     TEXT,
    name            VARCHAR(64),
    gender          VARCHAR(8),
    age             INT,
    phone           VARCHAR(20),
    email           VARCHAR(128),
    current_city    VARCHAR(32),
    intention_cities VARCHAR(256),                -- JSON 数组
    education       VARCHAR(16),
    school          VARCHAR(64),
    major           VARCHAR(64),
    work_years      INT,
    expected_salary_min INT,
    expected_salary_max INT,
    self_evaluation TEXT,
    raw_parse_json  TEXT,                         -- 完整解析结果 JSON
    embedding       BLOB,                         -- 向量
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES sys_user(id)
);
CREATE INDEX idx_resume_user ON resume(user_id);
CREATE INDEX idx_resume_status ON resume(parse_status);
CREATE INDEX idx_resume_city ON resume(current_city);
```

#### 5.2.3 职位表 `job`

```sql
CREATE TABLE job (
    id                  BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id             BIGINT NOT NULL,
    doc_url             VARCHAR(256),
    title               VARCHAR(128) NOT NULL,
    company             VARCHAR(128),
    department          VARCHAR(64),
    job_type            VARCHAR(16),
    salary_min          INT,
    salary_max          INT,
    salary_unit         VARCHAR(8),
    work_city           VARCHAR(32),
    experience_required VARCHAR(32),
    education_required  VARCHAR(16),
    headcount           INT DEFAULT 1,
    status              TINYINT DEFAULT 1,        -- 0=下架 1=招聘中 2=草稿
    raw_parse_json      TEXT,
    embedding           BLOB,
    created_at          TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES sys_user(id)
);
CREATE INDEX idx_job_user ON job(user_id);
CREATE INDEX idx_job_city ON job(work_city);
CREATE INDEX idx_job_status ON job(status);
```

#### 5.2.4 技能词典表 `skill_dict`

```sql
CREATE TABLE skill_dict (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    name        VARCHAR(64) NOT NULL UNIQUE,
    category    VARCHAR(32),                     -- 编程语言/框架/工具/软技能
    aliases     VARCHAR(256),                    -- 别名 JSON 数组
    embedding   BLOB
);
```

#### 5.2.5 简历技能关联表 `resume_skill`

```sql
CREATE TABLE resume_skill (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    resume_id   BIGINT NOT NULL,
    skill_name  VARCHAR(64) NOT NULL,
    skill_level VARCHAR(16),                     -- 精通/熟练/掌握/了解
    weight      FLOAT DEFAULT 0.6,
    FOREIGN KEY (resume_id) REFERENCES resume(id)
);
CREATE INDEX idx_rs_resume ON resume_skill(resume_id);
CREATE INDEX idx_rs_skill ON resume_skill(skill_name);
```

#### 5.2.6 职位要求表 `job_requirement`

```sql
CREATE TABLE job_requirement (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    job_id      BIGINT NOT NULL,
    skill_name  VARCHAR(64) NOT NULL,
    skill_level VARCHAR(16),
    req_type    VARCHAR(8),                      -- 必须/优先
    weight      FLOAT DEFAULT 1.0,
    FOREIGN KEY (job_id) REFERENCES job(id)
);
CREATE INDEX idx_jr_job ON job_requirement(job_id);
CREATE INDEX idx_jr_skill ON job_requirement(skill_name);
```

#### 5.2.7 匹配记录表 `match_record`

```sql
CREATE TABLE match_record (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    resume_id       BIGINT NOT NULL,
    job_id          BIGINT NOT NULL,
    total_score     FLOAT NOT NULL,
    skill_score     FLOAT,
    exp_score       FLOAT,
    edu_score       FLOAT,
    city_score      FLOAT,
    proj_score      FLOAT,
    salary_score    FLOAT,
    semantic_score  FLOAT,
    match_reason    TEXT,                        -- 豆包模型生成的自然语言依据
    direction       VARCHAR(16),                 -- JOB_TO_RESUME / RESUME_TO_JOB
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (resume_id) REFERENCES resume(id),
    FOREIGN KEY (job_id) REFERENCES job(id)
);
CREATE INDEX idx_mr_resume ON match_record(resume_id, total_score DESC);
CREATE INDEX idx_mr_job ON match_record(job_id, total_score DESC);
```

#### 5.2.8 操作日志表 `admin_log`

```sql
CREATE TABLE admin_log (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    admin_id        BIGINT NOT NULL,
    action          VARCHAR(64),
    target_type     VARCHAR(32),
    target_id       BIGINT,
    detail          TEXT,
    ip              VARCHAR(64),
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_al_admin ON admin_log(admin_id);
CREATE INDEX idx_al_time ON admin_log(created_at);
```

### 5.3 知识图谱存储 (Neo4j)

**索引与约束**:

```cypher
CREATE CONSTRAINT person_id IF NOT EXISTS
FOR (n:Person) REQUIRE n.id IS UNIQUE;

CREATE CONSTRAINT skill_name IF NOT EXISTS
FOR (n:Skill) REQUIRE n.name IS UNIQUE;

CREATE INDEX job_id IF NOT EXISTS FOR (n:Job) ON (n.id);
```

**典型查询 — 获取个人能力图谱**:

```cypher
MATCH (p:Person {id: $person_id})-[r]-(n)
OPTIONAL MATCH (n)-[r2:SKILL_RELATED]-(s:Skill)
RETURN p, r, n, r2, s
```

**典型查询 — 技能匹配召回**:

```cypher
MATCH (j:Job {id: $job_id})-[r:REQUIRES_SKILL]->(sk:Skill)<-[r2:HAS_SKILL]-(p:Person)
RETURN p.id AS person_id,
       count(sk) AS hit_count,
       sum(r.weight * r2.weight) AS weighted_score
ORDER BY weighted_score DESC
LIMIT 50;
```

---

## 6. API 接口设计

### 6.1 接口规范

- **风格**: RESTful
- **传输**: HTTPS (生产) / HTTP (开发)
- **认证**: Bearer JWT (Header: `Authorization: Bearer <token>`)
- **版本**: URL 路径版本 `/api/v1/`
- **响应格式**: 统一 JSON Envelope

**统一响应结构**:

```json
{
  "code": 0,
  "message": "success",
  "data": { },
  "trace_id": "uuid-v4",
  "timestamp": 1773307985
}
```

**错误码定义**:

| code | 含义 |
|------|------|
| 0 | 成功 |
| 1001 | 参数校验失败 |
| 1002 | 未认证 |
| 1003 | 无权限 |
| 1004 | 资源不存在 |
| 1005 | 账号已存在 |
| 1006 | 密码错误 |
| 2001 | 文件格式不支持 |
| 2002 | 文件超过大小限制 |
| 2003 | 文档解析失败 |
| 3001 | AI 服务不可用 |
| 5000 | 系统内部错误 |

### 6.2 完整接口列表

#### 6.2.1 认证模块

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/v1/auth/register` | 注册 | 否 |
| POST | `/api/v1/auth/login` | 登录 | 否 |
| POST | `/api/v1/auth/refresh` | 刷新 Token | 是 |
| POST | `/api/v1/auth/logout` | 退出 | 是 |

**注册请求示例**:

```json
POST /api/v1/auth/register
{
  "username": "zhangsan",
  "password": "Zs@123456",
  "role": "ROLE_SEEKER",
  "phone": "13800138000",
  "nickname": "张三",
  "real_name": "张三",
  "gender": "male"
}
```

**注册响应**:

```json
{
  "code": 0,
  "message": "注册成功",
  "data": {
    "user_id": 1001,
    "username": "zhangsan",
    "role": "ROLE_SEEKER",
    "access_token": "eyJhbGciOiJIUzI1NiIs...",
    "refresh_token": "eyJhbGciOiJIUzI1NiIs...",
    "expires_in": 7200
  }
}
```

**登录请求/响应**:

```json
POST /api/v1/auth/login
{
  "account": "13800138000",     // 手机号/邮箱/用户名
  "password": "Zs@123456"
}

响应:
{
  "code": 0,
  "data": {
    "user_id": 1001,
    "username": "zhangsan",
    "nickname": "张三",
    "role": "ROLE_SEEKER",
    "avatar_url": "/avatars/1001.jpg",
    "access_token": "eyJhbGciOiJIUzI1NiIs...",
    "refresh_token": "eyJhbGciOiJIUzI1NiIs...",
    "expires_in": 7200
  }
}
```

#### 6.2.2 用户模块

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/users/profile` | 获取当前用户信息 |
| PUT | `/api/v1/users/profile` | 更新用户信息 |
| PUT | `/api/v1/users/password` | 修改密码 |
| POST | `/api/v1/users/avatar` | 上传头像 |

#### 6.2.3 文档与简历模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/documents/upload` | 上传 DOC/PDF |
| GET | `/api/v1/documents/{id}/status` | 解析状态轮询 |
| GET | `/api/v1/documents/{id}/result` | 解析结果 |
| GET | `/api/v1/resumes/me` | 我的简历列表 |
| GET | `/api/v1/resumes/{id}` | 简历详情 |
| DELETE | `/api/v1/resumes/{id}` | 删除简历 |

#### 6.2.4 职位模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/jobs` | 发布职位 |
| GET | `/api/v1/jobs/me` | 企业职位列表 |
| GET | `/api/v1/jobs` | 职位广场(搜索) |
| GET | `/api/v1/jobs/{id}` | 职位详情 |
| PUT | `/api/v1/jobs/{id}` | 编辑职位 |
| DELETE | `/api/v1/jobs/{id}` | 删除职位 |

#### 6.2.5 图谱模块

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/graph/resume/{id}` | 个人能力图谱 |
| GET | `/api/v1/graph/job/{id}` | 职位能力图谱 |
| GET | `/api/v1/graph/match/{r_id}/{j_id}` | 匹配对比图谱 |

#### 6.2.6 匹配模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/match/recommend-jobs` | 推荐职位(个人) |
| POST | `/api/v1/match/recommend-candidates` | 推荐候选人(企业) |
| GET | `/api/v1/match/detail` | 匹配详情 |

**匹配推荐请求示例**:

```json
POST /api/v1/match/recommend-jobs
{
  "resume_id": 1783307985001,
  "top_k": 10,
  "filters": {
    "city": ["杭州", "上海"],
    "salary_min": 15000,
    "job_type": "全职"
  }
}
```

**匹配推荐响应**:

```json
{
  "code": 0,
  "data": {
    "recommendations": [
      {
        "job_id": 1783307985020,
        "title": "高级 Python 开发工程师",
        "company": "XX 科技有限公司",
        "work_city": "杭州",
        "salary_range": "20K-35K",
        "total_score": 87.5,
        "scores": {
          "skill": 0.92,
          "experience": 0.85,
          "city": 1.0,
          "project": 0.80
        },
        "match_reason": "命中必须技能: Python(精通)、FastAPI(熟练); 6年经验满足5年要求; 意向城市杭州匹配; 含2个微服务项目经验。",
        "matched_skills": ["Python", "FastAPI", "Redis", "Docker"],
        "missing_skills": ["Kubernetes"]
      }
    ],
    "total": 1
  }
}
```

#### 6.2.7 管理后台模块

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/admin/users` | 用户列表 |
| PUT | `/api/v1/admin/users/{id}/status` | 启用/禁用用户 |
| GET | `/api/v1/admin/resumes` | 简历列表 |
| POST | `/api/v1/admin/resumes/{id}/reparse` | 重新解析 |
| GET | `/api/v1/admin/jobs` | 职位列表 |
| GET | `/api/v1/admin/dashboard/stats` | 看板统计 |
| GET | `/api/v1/admin/logs/operations` | 操作日志 |

---

## 7. 前端完整页面设计

### 7.1 设计原则

| 原则 | 说明 |
|------|------|
| **直观性** | 关键操作 ≤ 3 步可达, 信息层级清晰 |
| **专业性** | 企业级深色侧边栏 + 浅色内容区, 符合 HR 审美 |
| **可视化** | 图谱、雷达图、进度环、热力图多维度呈现 |
| **响应式** | 适配 1280px+ PC, 关键页面兼容平板 |
| **一致性** | Element Plus 主题统一, 8px 栅格系统 |

### 7.2 视觉主题

**色彩系统**:

```scss
// 主色 — 科技蓝 (信任、专业)
$primary:        #1677FF;
$primary-hover:  #4096FF;
$primary-active: #0958D9;

// 辅助色
$success: #52C41A;   // 匹配度高
$warning: #FAAD14;   // 匹配度中
$danger:  #FF4D4F;   // 匹配度低 / 错误
$info:    #722ED1;   // 图谱节点

// 中性色
$bg-page:      #F5F7FA;
$bg-card:      #FFFFFF;
$border:       #E5E7EB;
$text-primary: #1F2937;
$text-regular: #4B5563;
$text-secondary: #9CA3AF;
```

**字体**:

```scss
$font-family: -apple-system, "PingFang SC", "Microsoft YaHei", "Helvetica Neue", Arial;
$font-size-base: 14px;
$font-size-lg: 16px;
$font-size-xl: 20px;
```

### 7.3 完整路由与页面清单

```
/login                           登录页
/register                        注册页(选择角色)
/forgot-password                 忘记密码

/seeker                           个人用户工作台
  ├── /seeker/dashboard           仪表盘
  ├── /seeker/resume/upload       简历上传
  ├── /seeker/resume/list         我的简历列表
  ├── /seeker/resume/:id          简历详情(结构化展示)
  ├── /seeker/resume/:id/graph    能力图谱可视化
  ├── /seeker/jobs                职位广场
  ├── /seeker/jobs/:id            职位详情
  ├── /seeker/match/:jobId        匹配度详情(对比图谱)
  └── /seeker/profile             个人设置

/employer                         企业用户工作台
  ├── /employer/dashboard         仪表盘
  ├── /employer/job/upload        职位文档上传
  ├── /employer/job/create        手动发布职位
  ├── /employer/job/list          职位列表
  ├── /employer/job/:id           职位详情
  ├── /employer/job/:id/graph     职位能力图谱
  ├── /employer/candidates        候选人推荐池
  ├── /employer/candidates/:id    候选人详情(匹配分析)
  └── /employer/profile           企业设置

/admin                            管理后台
  ├── /admin/dashboard            运营监控看板
  ├── /admin/users                用户管理
  ├── /admin/resumes              简历管理
  ├── /admin/jobs                 职位管理
  ├── /admin/graph                图谱管理
  ├── /admin/skills               技能词典
  ├── /admin/logs                 操作日志
  └── /admin/settings             系统配置
```

### 7.4 登录页完整设计 `/login`

#### 7.4.1 页面布局

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│  ┌──────────────────────────┐  ┌──────────────────────────────┐ │
│  │                          │  │                              │ │
│  │   品牌展示区 (左 55%)     │  │   登录表单区 (右 45%)        │ │
│  │                          │  │                              │ │
│  │   ┌──────────────────┐   │  │   欢迎登录                   │ │
│  │   │  智聘云图 LOGO    │   │  │   智聘云图 · 人才智能匹配    │ │
│  │   │  (动态图谱动画)   │   │  │                              │ │
│  │   └──────────────────┘   │  │   ┌────────────────────────┐ │ │
│  │                          │  │   │ 📱 手机号/邮箱/用户名   │ │ │
│  │   "AI 驱动的智能招聘平台" │  │   └────────────────────────┘ │ │
│  │                          │  │   ┌────────────────────────┐ │ │
│  │   特性亮点:              │  │   │ 🔒 密码          👁 显示│ │ │
│  │   ✓ 文档智能解析          │  │   └────────────────────────┘ │ │
│  │   ✓ 能力图谱可视化        │  │   ┌────────┐ ┌─────────────┐│ │
│  │   ✓ 双向精准匹配          │  │   │ 记住我 │ │ 忘记密码?   ││ │
│  │   ✓ 10万+ 人才库          │  │   └────────┘ └─────────────┘│ │
│  │                          │  │   ┌────────────────────────┐ │ │
│  │   (背景: 科技蓝渐变 +    │  │   │       登  录           │ │ │
│  │    知识图谱节点动效)     │  │   └────────────────────────┘ │ │
│  │                          │  │                              │ │
│  │                          │  │   还没有账号? 立即注册 >     │ │
│  └──────────────────────────┘  └──────────────────────────────┘ │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

#### 7.4.2 表单字段与校验

| 字段 | 类型 | 校验规则 |
|------|------|---------|
| 账号 | text | 必填, 支持手机号/邮箱/用户名格式 |
| 密码 | password | 必填, 6-20 位 |
| 记住我 | checkbox | 勾选后 Token 有效期 7 天, 否则 2 小时 |

#### 7.4.3 交互行为

1. **输入实时校验**: 失焦时校验格式, 错误下方红字提示
2. **密码可见切换**: 点击 👁 图标切换明文/密文
3. **登录按钮**: 表单合法时高亮可点击, 提交时显示 loading + 禁用
4. **登录失败**: Element Plus `ElMessage.error` 弹出错误提示 (如 "账号或密码错误")
5. **登录成功**: 
   - 存储 token 到 Pinia + localStorage
   - `ElMessage.success("登录成功")`
   - 根据 `role` 字段跳转对应工作台
6. **回车提交**: 密码框按 Enter 触发登录

#### 7.4.4 关键代码结构

```vue
<!-- views/auth/Login.vue -->
<template>
  <div class="login-container">
    <BrandPanel />          <!-- 左侧品牌区 -->
    <div class="login-form-panel">
      <h2>欢迎登录</h2>
      <p class="subtitle">智聘云图 · 人才智能匹配平台</p>
      <el-form :model="form" :rules="rules" ref="formRef">
        <el-form-item prop="account">
          <el-input v-model="form.account" placeholder="手机号/邮箱/用户名" prefix-icon="User" />
        </el-form-item>
        <el-form-item prop="password">
          <el-input v-model="form.password" type="password" placeholder="密码"
                    prefix-icon="Lock" show-password @keyup.enter="handleLogin" />
        </el-form-item>
        <div class="login-options">
          <el-checkbox v-model="form.remember">记住我</el-checkbox>
          <router-link to="/forgot-password">忘记密码?</router-link>
        </div>
        <el-button type="primary" class="login-btn" :loading="loading"
                   @click="handleLogin">登 录</el-button>
        <div class="register-link">
          还没有账号? <router-link to="/register">立即注册 ></router-link>
        </div>
      </el-form>
    </div>
  </div>
</template>
```

### 7.5 注册页完整设计 `/register`

#### 7.5.1 页面布局

```
┌──────────────────────────────────────────────────────────────────┐
│  ┌──────────────────────────┐  ┌──────────────────────────────┐ │
│  │   品牌展示区 (同登录)     │  │   注册表单区                 │ │
│  │                          │  │                              │ │
│  │   "加入智聘云图"          │  │   选择身份:                  │ │
│  │   "开启智能招聘新体验"    │  │   ┌──────────┐ ┌──────────┐ │ │
│  │                          │  │   │  个人用户 │ │ 企业用户  │ │ │
│  │                          │  │   │   👤     │ │   🏢     │ │ │
│  │                          │  │   │ 求职找工作│ │ 招聘人才  │ │ │
│  │                          │  │   └──────────┘ └──────────┘ │ │
│  │                          │  │                              │ │
│  │                          │  │   ┌────────────────────────┐ │ │
│  │                          │  │   │ 用户名                 │ │ │
│  │                          │  │   └────────────────────────┘ │ │
│  │                          │  │   ┌────────────────────────┐ │ │
│  │                          │  │   │ 手机号                 │ │ │
│  │                          │  │   └────────────────────────┘ │ │
│  │                          │  │   ┌────────────────────────┐ │ │
│  │                          │  │   │ 邮箱                   │ │ │
│  │                          │  │   └────────────────────────┘ │ │
│  │                          │  │   ┌────────────────────────┐ │ │
│  │                          │  │   │ 密码 (6-20位)          │ │ │
│  │                          │  │   └────────────────────────┘ │ │
│  │                          │  │   ┌────────────────────────┐ │ │
│  │                          │  │   │ 确认密码               │ │ │
│  │                          │  │   └────────────────────────┘ │ │
│  │                          │  │                              │ │
│  │                          │  │   【个人用户额外字段】        │ │
│  │                          │  │   姓名 / 性别 / 昵称         │ │
│  │                          │  │   【企业用户额外字段】        │ │
│  │                          │  │   企业名称 / 信用代码 / 联系人│ │
│  │                          │  │                              │ │
│  │                          │  │   ☐ 我已阅读并同意《用户协议》│ │
│  │                          │  │   ┌────────────────────────┐ │ │
│  │                          │  │   │       注  册           │ │ │
│  │                          │  │   └────────────────────────┘ │ │
│  │                          │  │   已有账号? 返回登录 >       │ │
│  └──────────────────────────┘  └──────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

#### 7.5.2 角色切换字段

**个人用户字段**:

| 字段 | 类型 | 校验 |
|------|------|------|
| 用户名 | text | 必填, 4-20 位字母数字下划线 |
| 手机号 | text | 必填, 11 位手机号格式 |
| 邮箱 | text | 选填, 邮箱格式 |
| 密码 | password | 必填, 6-20 位, 含字母+数字 |
| 确认密码 | password | 必填, 与密码一致 |
| 真实姓名 | text | 选填 |
| 性别 | radio | 男/女 |
| 昵称 | text | 选填 |
| 用户协议 | checkbox | 必须勾选 |

**企业用户字段**:

| 字段 | 类型 | 校验 |
|------|------|------|
| 用户名 | text | 必填, 4-20 位 |
| 手机号 | text | 必填, 11 位 |
| 邮箱 | text | 选填 |
| 密码 | password | 必填, 6-20 位 |
| 确认密码 | password | 必填, 一致 |
| 企业名称 | text | 必填 |
| 统一社会信用代码 | text | 必填, 18 位 |
| 联系人 | text | 必填 |
| 用户协议 | checkbox | 必须勾选 |

#### 7.5.3 交互行为

1. **角色切换**: 点击卡片切换, 表单额外字段动态显示/隐藏 (带过渡动画)
2. **密码强度**: 实时显示密码强度条 (弱/中/强)
3. **用户名实时校验**: 失焦时调用后端检查唯一性
4. **注册成功**: 
   - `ElMessage.success("注册成功, 即将跳转登录")`
   - 2 秒后跳转 `/login`, 自动填充用户名

### 7.6 个人用户工作台设计

#### 7.6.1 个人仪表盘 `/seeker/dashboard`

```
┌──────────────────────────────────────────────────────────────────┐
│  顶部导航: Logo + 面包屑 + 通知铃铛 + 用户头像下拉              │
├──────────────────────────────────────────────────────────────────┤
│ ┌─ 侧边栏 ─┐ ┌──────────── 主内容区 ─────────────────────────┐  │
│ │          │ │                                                │  │
│ │ 📊 仪表盘│ │  欢迎, 张三!                                   │  │
│ │ 📄 简历  │ │  ┌──────────┐ ┌──────────┐ ┌──────────┐      │  │
│ │  ├ 上传  │ │  │ 简历数   │ │ 推荐职位 │ │ 平均匹配度│     │  │
│ │  ├ 列表  │ │  │    3     │ │   28     │ │  78.5%   │      │  │
│ │ 📋 职位  │ │  └──────────┘ └──────────┘ └──────────┘      │  │
│ │ 🎯 匹配  │ │                                                │  │
│ │ ⚙ 设置  │ │  ┌──────── 简历完整度 ────────┐                │  │
│ │          │ │  │  ████████████░░░ 85%       │                │  │
│ │          │ │  │  补充项目经历可提升至 95%   │                │  │
│ │          │ │  └────────────────────────────┘                │  │
│ │          │ │                                                │  │
│ │          │ │  ┌──── 推荐职位 Top 5 ────┐ ┌── 能力雷达 ──┐  │  │
│ │          │ │  │ 高级Python工程师 87分  │ │    技能       │  │  │
│ │          │ │  │ 全栈工程师 82分        │ │   ●           │  │
│ │          │ │  │ 数据工程师 78分        │ │ 经验 ●        │  │
│ │          │ │  │ ...                    │ │      ● 学历   │  │
│ │          │ │  └────────────────────────┘ └──────────────┘  │  │
│ │          │ │                                                │  │
│ │          │ │  ┌──── 能力图谱缩略图 (点击查看完整图谱) ────┐ │  │
│ │          │ │  │            [力导引图缩略]                 │ │  │
│ │          │ │  └────────────────────────────────────────────┘ │  │
│ └──────────┘ └────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

#### 7.6.2 简历上传页 `/seeker/resume/upload`

```
┌──────────────────────────────────────────────────────────────┐
│  上传简历                                                    │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │                                                        │ │
│  │              📄                                        │ │
│  │        拖拽文件到此处, 或点击上传                      │ │
│  │                                                        │ │
│  │      支持 DOC / DOCX / PDF 格式, 单文件 ≤ 20MB        │ │
│  │                                                        │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  解析进度:                                                   │
│  ✓ 文件上传完成                                              │
│  ✓ 内容提取完成                                              │
│  ⏳ AI 智能解析中... (预计 8 秒)                             │
│  ○ 结构化入库                                                │
│  ○ 能力图谱构建                                              │
│                                                              │
│  历史上传:                                                   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 张三_简历.pdf  |  已解析  |  2026-07-05  |  查看/删除│   │
│  └──────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

**交互**:
- 支持拖拽上传与点击上传
- 上传后实时显示解析进度条 (轮询 `/documents/{id}/status`)
- 解析完成后显示 "查看简历" 与 "查看能力图谱" 按钮

#### 7.6.3 简历列表页 `/seeker/resume/list`

```
┌──────────────────────────────────────────────────────────────┐
│  我的简历                              [+ 上传新简历]        │
├──────────────────────────────────────────────────────────────┤
│  筛选: [状态 ▼] [时间 ▼]           搜索: [____________]     │
├──────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 📄 张三_简历.pdf                                       │ │
│  │ 张三 | 6年经验 | 本科 | 杭州 | 解析成功               │ │
│  │ 技能: Python, FastAPI, Vue3, Docker...                │ │
│  │ 上传时间: 2026-07-05 14:30                            │ │
│  │ [查看详情] [能力图谱] [重新解析] [删除]               │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 📄 张三_简历_v2.docx                                   │ │
│  │ ...                                                    │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  分页: < 1 2 3 >                                            │
└──────────────────────────────────────────────────────────────┘
```

#### 7.6.4 简历详情页 `/seeker/resume/:id`

结构化展示解析结果, 分区块:
- **基本信息卡**: 姓名、性别、年龄、联系方式、城市、学历
- **技能标签云**: 不同等级技能用不同颜色标签
- **工作经历时间轴**: 垂直时间轴展示
- **项目经历卡片**: 网格布局
- **证书列表**
- **自我评价**

#### 7.6.5 能力图谱可视化页 `/seeker/resume/:id/graph`

```
┌──────────────────────────────────────────────────────────────┐
│  能力图谱 - 张三            [力导引] [同心圆]  [导出] [全屏]│
├──────┬───────────────────────────────────────────────────────┤
│      │                                                       │
│ 筛选 │                                                       │
│ ☑ 技能│              ●─────●                                 │
│ ☑ 经历│             / \                                     │
│ ☑ 项目│        ●───张三───●                                 │
│ ☑ 教育│             \ / \                                   │
│ ☑ 证书│              ●   ●                                  │
│      │                                                       │
│ 图例 │     (节点可拖拽, 滚轮缩放, 点击查看详情)             │
│ ● 个人│                                                       │
│ ⬢ 技能│                                                       │
│ ▢ 经历│                                                       │
│      │                                                       │
├──────┴───────────────────────────────────────────────────────┤
│  选中节点详情: Python (技能)                                 │
│  等级: 精通 | 分类: 编程语言 | 关联项目: 微服务重构          │
└──────────────────────────────────────────────────────────────┘
```

#### 7.6.6 职位广场页 `/seeker/jobs`

```
┌──────────────────────────────────────────────────────────────┐
│  职位广场                                                    │
├──────────────────────────────────────────────────────────────┤
│  搜索: [职位/公司/技能 ____________] [搜索]                  │
│  筛选: 城市[▼] 薪资[▼] 经验[▼] 学历[▼] 职位类型[▼]          │
├──────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 高级 Python 开发工程师          20K-35K · 杭州          │ │
│  │ XX 科技有限公司 · 全职 · 5-7年 · 本科                  │ │
│  │ 技能: Python FastAPI Redis Docker Kubernetes          │ │
│  │                              [查看详情] [匹配度分析]   │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 全栈工程师                      18K-30K · 上海          │ │
│  │ ...                                                    │ │
│  └────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

#### 7.6.7 匹配度详情页 `/seeker/match/:jobId`

```
┌──────────────────────────────────────────────────────────────┐
│  匹配度分析 - 高级 Python 开发工程师                         │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│              ┌─────────────────┐                             │
│              │                 │                             │
│              │      87.5       │   匹配度: 优秀              │
│              │      /100       │                             │
│              │                 │                             │
│              └─────────────────┘                             │
│                                                              │
│  AI 分析: 命中必须技能 Python(精通)、FastAPI(熟练)...       │
│                                                              │
├──────────────────────────┬───────────────────────────────────┤
│  技能匹配对比             │  多维度雷达图                     │
│  ┌────────────────────┐  │              技能                 │
│  │ Python    必须 ✓精通│  │            ●                     │
│  │ FastAPI   必须 ✓熟练│  │         经验 ●                   │
│  │ Redis     优先 ✓掌握│  │       ●         ● 城市           │
│  │ Docker    优先 ✓熟练│  │     学历           项目          │
│  │ K8s       必须 ✗    │  │         ●                        │
│  └────────────────────┘  │                                  │
├──────────────────────────┴───────────────────────────────────┤
│  匹配对比图谱 (个人图谱 ∩ 职位图谱, 高亮重合节点)            │
│  [AntV G6 双图对比可视化]                                    │
└──────────────────────────────────────────────────────────────┘
```

### 7.7 企业用户工作台设计

#### 7.7.1 企业仪表盘 `/employer/dashboard`

```
┌──────────────────────────────────────────────────────────────┐
│  欢迎, XX 科技有限公司                                       │
├──────────────────────────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 在招职位 │ │ 收到简历 │ │ 推荐候选人│ │ 平均匹配度│      │
│  │   12     │ │   156    │ │   48     │ │  72.3%   │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│                                                              │
│  ┌──── 候选人推荐 Top 5 ────┐  ┌── 职位能力图谱 ──┐        │
│  │ 张三 87分 Python工程师    │  │   [图谱缩略]      │        │
│  │ 李四 82分 ...             │  │                   │        │
│  │ 王五 78分 ...             │  └───────────────────┘        │
│  └───────────────────────────┘                              │
└──────────────────────────────────────────────────────────────┘
```

#### 7.7.2 职位上传页 `/employer/job/upload`

与简历上传页结构类似, 上传职位描述 DOC/PDF, 显示解析进度, 解析完成后可编辑后发布。

#### 7.7.3 候选人推荐池 `/employer/candidates`

```
┌──────────────────────────────────────────────────────────────┐
│  候选人推荐             职位: [高级Python工程师 ▼]           │
├──────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 张三  |  87分  |  6年经验 | 杭州 | 本科 | Python精通   │ │
│  │ 命中技能: Python FastAPI Redis Docker                  │ │
│  │ [查看详情] [下载简历] [邀请面试]                       │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 李四  |  82分  |  ...                                  │ │
│  └────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

### 7.8 管理后台设计

#### 7.8.1 运营监控看板 `/admin/dashboard`

```
┌──────────────────────────────────────────────────────────────┐
│  运营监控看板                                                │
├──────────────────────────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 总用户数 │ │ 简历总数 │ │ 职位总数 │ │ 今日匹配 │       │
│  │ 12,580   │ │ 108,432  │ │  8,765   │ │   342    │       │
│  │ ↑ 2.3%   │ │ ↑ 5.1%   │ │ ↑ 1.8%   │ │ ↑ 12%    │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│                                                              │
│  ┌──── 近 30 天趋势 (折线图) ────┐  ┌── 用户角色占比 ──┐   │
│  │  注册 / 上传 / 匹配 三条曲线   │  │   个人 78%        │   │
│  │                                │  │   企业 20%        │   │
│  │                                │  │   管理员 2%       │   │
│  └────────────────────────────────┘  └───────────────────┘   │
│                                                              │
│  ┌──── 各城市简历分布 (柱状图) ──┐  ┌── 技能热度 Top20 ──┐  │
│  │  北京 ████████████            │  │  (热力图)            │  │
│  │  上海 ██████████              │  │  Python ████████     │  │
│  │  杭州 ██████                  │  │  Java ███████        │  │
│  └────────────────────────────────┘  └──────────────────────┘ │
│                                                              │
│  ┌──── 实时解析任务队列 ────┐  ┌──── 系统资源监控 ────┐     │
│  │ task_001 解析中 3s       │  │ CPU: 45% ████░░     │     │
│  │ task_002 排队中          │  │ 内存: 62% ██████░   │     │
│  │ task_003 解析中 6s       │  │ 磁盘: 38% ████░░    │     │
│  └───────────────────────────┘  └──────────────────────┘     │
└──────────────────────────────────────────────────────────────┘
```

#### 7.8.2 用户管理页 `/admin/users`

```
┌──────────────────────────────────────────────────────────────┐
│  用户管理                              [批量导入] [导出]    │
├──────────────────────────────────────────────────────────────┤
│  筛选: 角色[▼] 状态[▼]  搜索: [用户名/手机号______]         │
├──────────────────────────────────────────────────────────────┤
│  用户名    | 角色     | 手机号      | 状态 | 注册时间 | 操作│
│  zhangsan  | 个人用户 | 138****0000 | 启用 | 07-05   | ... │
│  company_a | 企业用户 | 139****1234 | 启用 | 07-04   | ... │
│  ...                                                          │
│  操作: [查看] [编辑] [启用/禁用] [重置密码]                  │
└──────────────────────────────────────────────────────────────┘
```

### 7.9 关键可视化组件

| 组件 | 技术 | 用途 |
|------|------|------|
| `BrandPanel.vue` | CSS 动画 | 登录注册左侧品牌区 |
| `CapabilityGraph.vue` | AntV G6 | 能力图谱关系图 |
| `MatchRadar.vue` | ECharts | 多维度匹配雷达图 |
| `ScoreRing.vue` | SVG + CSS | 匹配分环形进度 |
| `SkillMatchBar.vue` | Element Plus | 技能命中对比条 |
| `DashboardCharts.vue` | ECharts | 后台看板图表组 |
| `UploadDropzone.vue` | 自研 | 拖拽上传组件 |
| `ParseProgress.vue` | Element Plus | 解析进度时间轴 |
| `ResumeTimeline.vue` | Element Plus | 工作经历时间轴 |
| `SkillTagCloud.vue` | 自研 | 技能标签云 |

---

## 8. AI 与知识图谱设计

### 8.1 豆包大模型服务架构

本平台 AI 能力核心依托 **火山引擎方舟 (ARK) 平台** 的豆包大模型, 通过兼容 OpenAI 协议的 API 调用。

#### 8.1.1 模型配置

```yaml
# config/ai_config.yaml
llm:
  provider: "volcengine_ark"
  base_url: "https://ark.cn-beijing.volces.com/api/v3"
  api_key: "ark-bde00d78-dc34-4aef-9985-345ac356fafe-45c4d"
  chat_model: "doubao-seed-2-0-lite-260428"          # 对话/解析模型
  embedding_model: "doubao-embedding-text-240915"     # 向量化模型
  max_tokens: 4096
  temperature: 0.1                                    # 解析任务低温度保证稳定
  timeout: 60

embedding:
  dimension: 2048                                     # 豆包 Embedding 维度
  batch_size: 16
```

#### 8.1.2 ARK Gateway 架构

```
┌─────────────────────────────────────────────────────┐
│              ARK Gateway (自研网关)                  │
│                                                     │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐       │
│  │ 路由策略  │   │ 限流熔断  │   │ 重试机制  │       │
│  └──────────┘   └──────────┘   └──────────┘       │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │         火山引擎 ARK API Provider            │   │
│  │                                             │   │
│  │  POST /api/v3/chat/completions              │   │
│  │   └─ doubao-seed-2-0-lite-260428            │   │
│  │      (简历解析/职位解析/匹配依据/重排序)     │   │
│  │                                             │   │
│  │  POST /api/v3/embeddings                    │   │
│  │   └─ doubao-embedding-text-240915           │   │
│  │      (简历向量化/职位向量化/技能向量化)      │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

#### 8.1.3 统一客户端封装 (Python)

```python
# backend/app/ai/ark_client.py
from openai import AsyncOpenAI
from typing import Optional
import json
import logging

logger = logging.getLogger(__name__)

class ArkClient:
    """火山引擎豆包大模型客户端 (兼容 OpenAI SDK)"""

    def __init__(
        self,
        api_key: str = "ark-bde00d78-dc34-4aef-9985-345ac356fafe-45c4d",
        base_url: str = "https://ark.cn-beijing.volces.com/api/v3",
        chat_model: str = "doubao-seed-2-0-lite-260428",
        embedding_model: str = "doubao-embedding-text-240915",
    ):
        self.client = AsyncOpenAI(api_key=api_key, base_url=base_url)
        self.chat_model = chat_model
        self.embedding_model = embedding_model

    async def chat(self, prompt: str, system: str = None,
                   temperature: float = 0.1,
                   response_format: Optional[dict] = None) -> str:
        """调用豆包模型对话"""
        messages = []
        if system:
            messages.append({"role": "system", "content": system})
        messages.append({"role": "user", "content": prompt})

        kwargs = {
            "model": self.chat_model,
            "messages": messages,
            "temperature": temperature,
            "max_tokens": 4096,
        }
        if response_format:
            kwargs["response_format"] = response_format

        try:
            resp = await self.client.chat.completions.create(**kwargs)
            return resp.choices[0].message.content
        except Exception as e:
            logger.error(f"ARK chat 调用失败: {e}")
            raise

    async def chat_json(self, prompt: str, system: str = None) -> dict:
        """调用豆包模型并解析 JSON 输出"""
        content = await self.chat(
            prompt=prompt,
            system=system,
            temperature=0.1,
            response_format={"type": "json_object"}
        )
        return json.loads(content)

    async def embed(self, text: str) -> list[float]:
        """调用豆包 Embedding 模型向量化"""
        try:
            resp = await self.client.embeddings.create(
                model=self.embedding_model,
                input=text[:8000]  # 截断保护
            )
            return resp.data[0].embedding
        except Exception as e:
            logger.error(f"ARK embedding 调用失败: {e}")
            raise


# 全局单例
ark_client = ArkClient()
```

### 8.2 文档解析 Prompt 设计

#### 8.2.1 简历解析 Prompt

```python
# backend/app/ai/prompts.py

RESUME_PARSE_SYSTEM = """你是一个专业的人力资源简历解析专家。
请将用户提供的简历文本解析为严格的 JSON 结构, 遵循以下要求:

1. 必须输出合法 JSON, 不得包含 Markdown 代码块标记或额外说明
2. 字段缺失时使用 null, 数组缺失时使用 []
3. 技能等级必须归一化为四档: 精通 / 熟练 / 掌握 / 了解
4. 日期格式统一为 YYYY-MM, 至今用 "present"
5. 技能名称使用通用规范名 (如 "JS" 归一化为 "JavaScript")
6. 城市名称使用规范名 (如 "杭州市" 归一化为 "杭州")

输出 JSON Schema:
{
  "basic_info": {
    "name": "string",
    "gender": "male|female",
    "age": int,
    "phone": "string",
    "email": "string",
    "current_city": "string",
    "intention_city": ["string"],
    "education": "博士|硕士|本科|大专|其他",
    "school": "string",
    "major": "string",
    "work_years": int,
    "expected_salary": {"min": int, "max": int}
  },
  "skills": [{"name": "string", "level": "精通|熟练|掌握|了解", "category": "string"}],
  "work_experience": [{
    "company": "string", "position": "string",
    "start_date": "YYYY-MM", "end_date": "YYYY-MM|present",
    "description": "string", "achievements": ["string"]
  }],
  "project_experience": [{
    "name": "string", "role": "string", "duration": "string",
    "description": "string", "tech_stack": ["string"]
  }],
  "certifications": ["string"],
  "self_evaluation": "string"
}"""

async def parse_resume(text: str) -> dict:
    """调用豆包模型解析简历"""
    prompt = f"请解析以下简历内容:\n\n{text[:6000]}"
    return await ark_client.chat_json(
        prompt=prompt,
        system=RESUME_PARSE_SYSTEM
    )
```

#### 8.2.2 职位解析 Prompt

```python
JOB_PARSE_SYSTEM = """你是一个专业的招聘岗位解析专家。
请将用户提供的职位描述文本解析为严格的 JSON 结构。

输出 JSON Schema:
{
  "job_basic": {
    "title": "string", "company": "string", "department": "string",
    "job_type": "全职|兼职|实习",
    "salary_range": {"min": int, "max": int, "unit": "月|年"},
    "work_city": "string", "experience_required": "string",
    "education_required": "string", "headcount": int
  },
  "responsibilities": ["string"],
  "requirements": [{"type": "必须|优先", "skill": "string", "level": "string"}],
  "benefits": ["string"],
  "keywords": ["string"]
}"""

async def parse_job(text: str) -> dict:
    return await ark_client.chat_json(
        prompt=f"请解析以下职位描述:\n\n{text[:6000]}",
        system=JOB_PARSE_SYSTEM
    )
```

#### 8.2.3 匹配依据生成 Prompt

```python
MATCH_REASON_SYSTEM = """你是资深招聘专家, 请根据候选人简历与职位的匹配数据,
生成一段自然语言的匹配依据说明 (80-150 字), 包含:
1. 命中的必须技能及等级
2. 工作年限满足情况
3. 城市匹配情况
4. 项目经验亮点

直接输出说明文字, 不要包含 JSON 或 Markdown 标记。"""

async def generate_match_reason(resume: dict, job: dict, scores: dict) -> str:
    prompt = f"""候选人: {resume.get('name')}, {resume.get('work_years')}年经验,
意向城市: {resume.get('intention_city')}, 居住地: {resume.get('current_city')}
技能: {[s['name'] for s in resume.get('skills', [])]}

职位: {job.get('title')}, 工作城市: {job.get('work_city')}
必须技能: {[r['skill'] for r in job.get('requirements', []) if r['type']=='必须']}

匹配得分: 技能{scores['skill']:.0%}, 经验{scores['experience']:.0%},
城市{scores['city']:.0%}, 项目{scores['project']:.0%}"""
    return await ark_client.chat(
        prompt=prompt, system=MATCH_REASON_SYSTEM, temperature=0.3
    )
```

### 8.3 知识图谱构建

#### 8.3.1 三元组抽取 Prompt

```python
TRIPLE_EXTRACT_SYSTEM = """从结构化数据中抽取知识图谱三元组。
关系类型限定: HAS_SKILL, WORKED_AT, PARTICIPATED, GRADUATED_FROM, HOLDS_CERT

输出 JSON 数组:
[{"h":"实体:类型", "r":"关系", "t":"实体:类型", "props":{}}]"""

async def extract_triples(structured_data: dict) -> list[dict]:
    return await ark_client.chat_json(
        prompt=f"输入数据:\n{json.dumps(structured_data, ensure_ascii=False)}",
        system=TRIPLE_EXTRACT_SYSTEM
    )
```

### 8.4 匹配算法实现

#### 8.4.1 召回阶段

```python
import numpy as np

async def recall_candidates(query_vec: list[float],
                             candidate_vecs: np.ndarray,
                             candidate_ids: list[int],
                             top_k: int = 200) -> list[int]:
    """Embedding 向量召回"""
    query_arr = np.array(query_vec)
    # 余弦相似度
    scores = np.dot(candidate_vecs, query_arr) / (
        np.linalg.norm(candidate_vecs, axis=1) * np.linalg.norm(query_arr) + 1e-8
    )
    top_idx = np.argsort(scores)[::-1][:top_k]
    return [candidate_ids[i] for i in top_idx]
```

#### 8.4.2 精排阶段 (豆包重排序)

```python
async def llm_rerank(resume: dict, candidates: list[dict]) -> list[dict]:
    """豆包模型重排序"""
    prompt = f"""作为资深招聘专家, 请根据候选人简历对候选职位进行匹配度排序。

简历摘要: 姓名{resume['name']}, {resume['work_years']}年经验,
技能: {', '.join(s['name'] for s in resume['skills'][:10])}

候选职位:
{json.dumps([{'id': c['id'], 'title': c['title'], 'city': c['work_city'],
              'skills': [r['skill'] for r in c['requirements'][:5]]}
             for c in candidates], ensure_ascii=False, indent=2)}

请输出按匹配度从高到低排序的职位 ID 列表 (JSON 数组)。"""
    result = await ark_client.chat_json(prompt=prompt)
    ranked_ids = result.get("ranked_ids", [])
    return [next(c for c in candidates if c["id"] == rid) for rid in ranked_ids]
```

### 8.5 Embedding 向量化服务

```python
async def vectorize_resume(resume_data: dict) -> list[float]:
    """将简历摘要文本向量化"""
    summary = f"{resume_data['name']} {resume_data['work_years']}年经验 "
    summary += " ".join(s['name'] for s in resume_data.get('skills', []))
    summary += f" {resume_data.get('self_evaluation', '')}"
    return await ark_client.embed(summary)

async def vectorize_job(job_data: dict) -> list[float]:
    """将职位摘要文本向量化"""
    summary = f"{job_data['title']} {job_data.get('work_city')} "
    summary += " ".join(r['skill'] for r in job_data.get('requirements', []))
    summary += " ".join(job_data.get('responsibilities', []))
    return await ark_client.embed(summary)
```

---

## 9. 核心业务时序图

### 9.1 用户注册登录时序

```
用户         前端(Vue3)       后端(FastAPI)      数据库       Redis
 │              │                  │                │            │
 │  填写注册    │                  │                │            │
 │─────────────▶│                  │                │            │
 │              │  POST /register  │                │            │
 │              │─────────────────▶│                │            │
 │              │                  │  查重          │            │
 │              │                  │───────────────▶│            │
 │              │                  │◀───────────────│            │
 │              │                  │  BCrypt加密    │            │
 │              │                  │  写入用户      │            │
 │              │                  │───────────────▶│            │
 │              │                  │  生成JWT       │            │
 │              │                  │  存session──────────────▶   │
 │              │  返回token+用户  │                │            │
 │              │◀─────────────────│                │            │
 │  跳转工作台  │                  │                │            │
 │◀─────────────│                  │                │            │
```

### 9.2 文档上传与解析时序

```
用户    前端     后端API    Celery Worker    豆包ARK API    数据库   Neo4j
 │       │         │            │                │            │       │
 │ 上传  │         │            │                │            │       │
 │──────▶│         │            │                │            │       │
 │       │ POST    │            │                │            │       │
 │       │/upload  │            │                │            │       │
 │       │────────▶│            │                │            │       │
 │       │         │ 存MinIO    │                │            │       │
 │       │         │ 入队任务   │                │            │       │
 │       │         │───────────▶│                │            │       │
 │       │ 返回doc │            │                │            │       │
 │       │◀────────│            │                │            │       │
 │ 轮询  │         │            │                │            │       │
 │──────▶│ GET/status           │                │            │       │
 │       │────────▶│            │                │            │       │
 │       │ 解析中  │            │                │            │       │
 │       │◀────────│            │                │            │       │
 │       │         │            │ 提取文本       │            │       │
 │       │         │            │ (docx/pdf/ocr) │            │       │
 │       │         │            │                │            │       │
 │       │         │            │ 调用豆包解析   │            │       │
 │       │         │            │───────────────▶│            │       │
 │       │         │            │  结构化JSON    │            │       │
 │       │         │            │◀───────────────│            │       │
 │       │         │            │                │            │       │
 │       │         │            │ 调用豆包Embedding───────────▶│       │
 │       │         │            │  向量          │            │       │
 │       │         │            │◀───────────────│            │       │
 │       │         │            │                            │       │
 │       │         │            │  写入结构化数据           │       │
 │       │         │            │──────────────────────────▶│       │
 │       │         │            │  构建图谱节点                     │
 │       │         │            │──────────────────────────────────▶│
 │       │         │            │  状态=成功                       │
 │       │ GET/status           │                │            │       │
 │       │────────▶│            │                │            │       │
 │       │ 解析成功│            │                │            │       │
 │       │◀────────│            │                │            │       │
 │ 显示结果       │            │                │            │       │
│◀───────│         │            │                │            │       │
```

### 9.3 智能匹配推荐时序

```
用户    前端     后端API    Match引擎    豆包ARK API    Neo4j    数据库
 │       │         │           │              │          │        │
 │ 请求推荐        │           │              │          │        │
 │──────▶│         │           │              │          │        │
 │       │ POST    │           │              │          │        │
 │       │/match   │           │              │          │        │
 │       │────────▶│           │              │          │        │
 │       │         │ 调用匹配  │              │          │        │
 │       │         │──────────▶│              │          │        │
 │       │         │           │ 1.向量召回    │          │        │
 │       │         │           │  (从DB取embedding)      │        │
 │       │         │           │─────────────────────────────────▶│
 │       │         │           │  Top200候选              │        │
 │       │         │           │◀─────────────────────────────────│
 │       │         │           │              │          │        │
 │       │         │           │ 2.图谱召回(技能重合)    │        │
 │       │         │           │─────────────────────────▶│        │
 │       │         │           │◀─────────────────────────│        │
 │       │         │           │              │          │        │
 │       │         │           │ 3.规则粗排   │          │        │
 │       │         │           │ (多维评分)   │          │        │
 │       │         │           │ Top50        │          │        │
 │       │         │           │              │          │        │
 │       │         │           │ 4.豆包重排序 │          │        │
 │       │         │           │─────────────▶│          │        │
 │       │         │           │◀─────────────│          │        │
 │       │         │           │ Top10        │          │        │
 │       │         │           │              │          │        │
 │       │         │           │ 5.生成匹配依据           │        │
 │       │         │           │─────────────▶│          │        │
 │       │         │           │◀─────────────│          │        │
 │       │         │           │              │          │        │
 │       │         │           │ 存match_record         │        │
 │       │         │           │─────────────────────────────────▶│
 │       │         │ 返回推荐  │              │          │        │
 │       │◀────────│           │              │          │        │
 │ 显示列表       │           │              │          │        │
 │◀──────│         │           │              │          │        │
```

---

## 10. 安全与性能设计

### 10.1 安全设计

| 维度 | 措施 |
|------|------|
| 传输安全 | 生产全站 HTTPS (TLS 1.3), HSTS 启用 |
| 身份认证 | JWT (access 2h + refresh 7d), BCrypt 密码哈希 (cost=12) |
| 授权控制 | RBAC 角色权限模型, 接口级 `@requires_role` 装饰器 |
| 文件安全 | 上传 MIME 白名单, 文件大小限制 20MB, 文件头校验 |
| 注入防护 | SQLAlchemy 参数化查询, Pydantic 输入校验, 输出转义 |
| 限流防刷 | Redis + 滑动窗口, 单 IP 60 req/min, 上传 10 次/h |
| 敏感数据 | 手机号/邮箱脱敏展示 (138****0000), 简历原文按权限可见 |
| 审计日志 | 管理员操作全量记录, 保留 180 天 |
| 密钥管理 | 环境变量 + `.env` 文件, 禁止硬编码 (ARK API Key 走环境变量) |
| CORS | 仅允许配置的前端域名 |

### 10.2 性能设计

| 场景 | 优化策略 | 目标 |
|------|---------|------|
| 文档解析 | Celery 异步队列, 并发 Worker=4 | P95 ≤ 10s |
| 匹配召回 | Embedding 预计算 + Redis 缓存 | 召回 ≤ 500ms |
| 图谱查询 | Neo4j 索引 + 查询结果缓存 5min | P95 ≤ 200ms |
| 列表分页 | 游标分页 (深分页优化) | ≤ 100ms |
| 静态资源 | Nginx gzip + 强缓存 | 首屏 ≤ 2s |
| 数据库 | 索引优化 + 连接池 | QPS ≥ 200 |
| AI 调用 | ARK API 异步并发 + 超时熔断 | 单次 ≤ 30s |

### 10.3 ARK API 调用容错

```python
# backend/app/ai/fallback.py
import asyncio
from functools import wraps

def ark_with_retry(max_retries=3, timeout=30):
    """ARK API 调用重试装饰器"""
    def decorator(func):
        @wraps(func)
        async def wrapper(*args, **kwargs):
            for attempt in range(max_retries):
                try:
                    return await asyncio.wait_for(
                        func(*args, **kwargs), timeout=timeout
                    )
                except Exception as e:
                    if attempt == max_retries - 1:
                        logger.error(f"ARK 调用失败 (重试{max_retries}次): {e}")
                        raise
                    await asyncio.sleep(2 ** attempt)  # 指数退避
        return wrapper
    return decorator
```

---

## 11. 部署与运维

### 11.1 开发环境 (Windows) 配置

#### 11.1.1 前端开发环境

```bash
# 安装 Node.js 20+ (Windows 安装包)
# https://nodejs.org/

# 创建前端项目
cd d:\智聘云图
npm create vite@latest frontend -- --template vue-ts
cd frontend
npm install
npm install element-plus vue-router pinia axios echarts @antv/g6
npm install -D tailwindcss postcss autoprefixer

# 启动开发服务器
npm run dev
# 访问 http://localhost:5173
```

#### 11.1.2 后端开发环境

```bash
# 安装 Python 3.11+ (Windows 安装包)
# https://www.python.org/downloads/

# 创建虚拟环境
cd d:\智聘云图
python -m venv venv
venv\Scripts\activate

# 安装依赖
pip install fastapi uvicorn sqlalchemy celery redis
pip install python-docx pdfplumber paddleocr
pip install openai python-jose passlib[bcrypt] python-multipart
pip install pydantic pydantic-settings

# 配置环境变量
copy .env.example .env
# 编辑 .env 填入 ARK_API_KEY 等

# 启动后端
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
# 访问 http://localhost:8000/docs (Swagger UI)
```

#### 11.1.3 开发环境 `.env.dev`

```bash
# 应用配置
APP_ENV=development
APP_DEBUG=true
SECRET_KEY=your-dev-secret-key-change-in-production

# 数据库 (开发用 SQLite)
DATABASE_URL=sqlite:///./zhipin.db

# Redis
REDIS_URL=redis://localhost:6379/0

# Neo4j
NEO4J_URL=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-password

# 文件存储 (开发用本地)
STORAGE_TYPE=local
STORAGE_PATH=./uploads

# 豆包 ARK API
ARK_API_KEY=ark-bde00d78-dc34-4aef-9985-345ac356fafe-45c4d
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
ARK_CHAT_MODEL=doubao-seed-2-0-lite-260428
ARK_EMBEDDING_MODEL=doubao-embedding-text-240915

# Celery (开发用 Redis broker)
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2
```

### 11.2 部署环境 (LoongArch + 麒麟) 配置

#### 11.2.1 部署拓扑

```
┌─────────────────────────────────────────────────────────┐
│   银河麒麟高级服务器 V11 (Loongarch 3A6000, 64C/128G)    │
│                                                         │
│  ┌──────────────── Docker Compose 编排 ──────────────┐ │
│  │                                                   │ │
│  │  nginx:1.24-loong64        ── 80/443             │ │
│  │  frontend:vue3-loong64     ── 8080 (内部)        │ │
│  │  backend:fastapi-loong64   ── 8000 (×2 实例)     │ │
│  │  celery-worker:loong64     ── (×4 并发)          │ │
│  │  dm8:8.1.2.128-loong64     ── 5236              │ │
│  │  neo4j:5.18-loong64        ── 7474/7687         │ │
│  │  redis:7.2-loong64         ── 6379              │ │
│  │  minio:loong64             ── 9000/9001         │ │
│  │  rabbitmq:3.13-loong64     ── 5672/15672        │ │
│  │  prometheus + grafana      ── 9090/3000         │ │
│  └───────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
            │
            │ 云端调用豆包 ARK API (无需本地模型)
            ▼
┌─────────────────────────────────────────────────────────┐
│   火山引擎方舟 ARK API (https://ark.cn-beijing.volces.com) │
└─────────────────────────────────────────────────────────┘
```

#### 11.2.2 生产环境 `.env.prod`

```bash
APP_ENV=production
APP_DEBUG=false
SECRET_KEY=${SECRET_KEY}

# 达梦 DM8 国产数据库
DATABASE_URL=dm+dmPython://SYSDBA:${DM_PASSWORD}@dm8:5236

REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0
NEO4J_URL=bolt://neo4j:${NEO4J_PASSWORD}@neo4j:7687

# MinIO 对象存储
STORAGE_TYPE=minio
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=${MINIO_USER}
MINIO_SECRET_KEY=${MINIO_PASSWORD}

# 豆包 ARK API (与开发环境相同)
ARK_API_KEY=ark-bde00d78-dc34-4aef-9985-345ac356fafe-45c4d
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
ARK_CHAT_MODEL=doubao-seed-2-0-lite-260428
ARK_EMBEDDING_MODEL=doubao-embedding-text-240915

CELERY_BROKER_URL=amqp://guest:${RABBIT_PASSWORD}@rabbitmq:5672
CELERY_RESULT_BACKEND=redis://:${REDIS_PASSWORD}@redis:6379/2
```

#### 11.2.3 Docker Compose 编排 (生产)

```yaml
version: "3.8"

services:
  nginx:
    image: loongarch64/nginx:1.24
    ports: ["80:80", "443:443"]
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./nginx/certs:/etc/nginx/certs
      - ./frontend/dist:/usr/share/nginx/html
    depends_on: [backend]

  backend:
    image: registry.loongnix.cn/zhipin/backend:1.0.0
    build: ./backend
    env_file: .env.prod
    expose: ["8000"]
    depends_on: [dm8, redis, neo4j, rabbitmq]
    deploy:
      replicas: 2
    restart: always

  celery-worker:
    image: registry.loongnix.cn/zhipin/backend:1.0.0
    command: celery -A app.celery_app worker -l info -c 4
    env_file: .env.prod
    depends_on: [rabbitmq, redis]
    deploy:
      replicas: 2

  dm8:
    image: loongarch64/dmdb:8.1.2.128
    environment:
      DM_USERNAME: SYSDBA
      DM_PASSWORD: ${DM_PASSWORD}
    volumes: ["dm8_data:/opt/dmdbms/data"]
    ports: ["5236:5236"]
    restart: always

  neo4j:
    image: loongarch64/neo4j:5.18-community
    environment:
      NEO4J_AUTH: neo4j/${NEO4J_PASSWORD}
    volumes: ["neo4j_data:/data"]
    ports: ["7474:7474", "7687:7687"]

  redis:
    image: loongarch64/redis:7.2
    command: redis-server --requirepass ${REDIS_PASSWORD}
    volumes: ["redis_data:/data"]

  minio:
    image: loongarch64/minio:latest
    environment:
      MINIO_ROOT_USER: ${MINIO_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_PASSWORD}
    volumes: ["minio_data:/data"]
    ports: ["9000:9000", "9001:9001"]

  rabbitmq:
    image: loongarch64/rabbitmq:3.13-management
    environment:
      RABBITMQ_DEFAULT_PASS: ${RABBIT_PASSWORD}
    ports: ["5672:5672", "15672:15672"]

volumes:
  dm8_data:
  neo4j_data:
  redis_data:
  minio_data:
```

#### 11.2.4 部署验证脚本

```bash
#!/bin/bash
# deploy_verify.sh - 智聘云图 LoongArch 部署验证

echo "[1/6] 校验 CPU 架构..."
ARCH=$(uname -m)
[ "$ARCH" == "loongarch64" ] || { echo "FAIL: 非 LoongArch 架构"; exit 1; }

echo "[2/6] 校验麒麟系统..."
grep -q "Kylin Linux Advanced Server" /etc/kylin-release || { echo "FAIL: 非麒麟高级服务器"; exit 1; }

echo "[3/6] 校验 Docker..."
docker info > /dev/null 2>&1 || { echo "FAIL: Docker 未运行"; exit 1; }

echo "[4/6] 启动服务栈..."
docker compose up -d

echo "[5/6] 等待服务就绪..."
sleep 30

echo "[6/6] 健康检查..."
curl -sf http://localhost/api/v1/health > /dev/null || { echo "FAIL: API 不可达"; exit 1; }

echo "SUCCESS: 智聘云图部署完成"
```

### 11.3 监控告警

| 指标 | 阈值 | 告警方式 |
|------|------|---------|
| API 5xx 错误率 | > 1% | 钉钉机器人 + 邮件 |
| ARK API 调用延迟 P95 | > 30s | 钉钉机器人 |
| 达梦 DB 连接数 | > 80% | 邮件 |
| 磁盘使用率 | > 85% | 邮件 + 钉钉 |
| Celery 队列堆积 | > 100 | 钉钉机器人 |

---

## 12. 实施计划与里程碑

### 12.1 里程碑划分

| 阶段 | 里程碑 | 交付物 |
|------|--------|--------|
| M1 | 基础设施搭建 | Windows 开发环境就绪, 前后端框架跑通 |
| M2 | 用户与认证模块 | 注册登录、JWT 鉴权、角色路由 |
| M3 | 文档上传与解析 | DOC/PDF 上传、豆包模型解析、结构化入库 |
| M4 | 图谱与匹配模块 | 能力图谱构建、双向推荐、可视化 |
| M5 | 管理后台 | 用户管理、监控看板、日志审计 |
| M6 | 联调与优化 | 端到端联调、性能压测、Bug 修复 |
| M7 | 部署上线 | LoongArch 部署、验证脚本、文档 |

### 12.2 项目目录结构

```
智聘云图/
├── frontend/                      # Vue3 前端
│   ├── src/
│   │   ├── api/                   # 接口封装 (axios)
│   │   │   ├── auth.ts
│   │   │   ├── resume.ts
│   │   │   ├── job.ts
│   │   │   ├── graph.ts
│   │   │   └── match.ts
│   │   ├── assets/                # 静态资源
│   │   ├── components/            # 通用组件
│   │   │   ├── BrandPanel.vue
│   │   │   ├── CapabilityGraph.vue
│   │   │   ├── MatchRadar.vue
│   │   │   ├── ScoreRing.vue
│   │   │   ├── UploadDropzone.vue
│   │   │   └── ParseProgress.vue
│   │   ├── layouts/               # 布局
│   │   │   ├── SeekerLayout.vue
│   │   │   ├── EmployerLayout.vue
│   │   │   └── AdminLayout.vue
│   │   ├── router/                # 路由
│   │   │   └── index.ts
│   │   ├── stores/                # Pinia 状态
│   │   │   ├── user.ts
│   │   │   └── resume.ts
│   │   ├── views/
│   │   │   ├── auth/              # 登录注册
│   │   │   │   ├── Login.vue
│   │   │   │   └── Register.vue
│   │   │   ├── seeker/            # 个人用户页
│   │   │   │   ├── Dashboard.vue
│   │   │   │   ├── ResumeUpload.vue
│   │   │   │   ├── ResumeList.vue
│   │   │   │   ├── ResumeDetail.vue
│   │   │   │   ├── ResumeGraph.vue
│   │   │   │   ├── JobSquare.vue
│   │   │   │   ├── JobDetail.vue
│   │   │   │   └── MatchDetail.vue
│   │   │   ├── employer/          # 企业用户页
│   │   │   │   ├── Dashboard.vue
│   │   │   │   ├── JobUpload.vue
│   │   │   │   ├── JobList.vue
│   │   │   │   ├── JobGraph.vue
│   │   │   │   └── Candidates.vue
│   │   │   └── admin/             # 管理后台
│   │   │       ├── Dashboard.vue
│   │   │       ├── Users.vue
│   │   │       ├── Resumes.vue
│   │   │       ├── Jobs.vue
│   │   │       └── Logs.vue
│   │   ├── utils/
│   │   └── App.vue
│   ├── Dockerfile
│   ├── vite.config.ts
│   └── package.json
├── backend/                       # FastAPI 后端
│   ├── app/
│   │   ├── api/v1/                # 路由
│   │   │   ├── auth.py
│   │   │   ├── users.py
│   │   │   ├── documents.py
│   │   │   ├── resumes.py
│   │   │   ├── jobs.py
│   │   │   ├── graph.py
│   │   │   ├── match.py
│   │   │   └── admin.py
│   │   ├── core/                  # 配置/安全/中间件
│   │   │   ├── config.py
│   │   │   ├── security.py
│   │   │   └── deps.py
│   │   ├── models/                # SQLAlchemy 模型
│   │   ├── schemas/               # Pydantic 模型
│   │   ├── services/              # 业务逻辑
│   │   ├── ai/                    # AI 引擎
│   │   │   ├── ark_client.py
│   │   │   ├── prompts.py
│   │   │   ├── parser.py
│   │   │   ├── embedder.py
│   │   │   ├── matcher.py
│   │   │   └── fallback.py
│   │   ├── graph/                 # 知识图谱
│   │   │   ├── builder.py
│   │   │   └── neo4j_client.py
│   │   ├── celery_app/            # 异步任务
│   │   │   ├── tasks.py
│   │   │   └── worker.py
│   │   └── main.py
│   ├── tests/
│   ├── Dockerfile
│   └── requirements.txt
├── deploy/                        # 部署文件
│   ├── docker-compose.yml
│   ├── nginx/
│   ├── .env.example
│   └── deploy_verify.sh
├── docs/
│   └── 智聘云图-软件设计文档.md
└── README.md
```

---

## 附录 A: 技术选型合规性说明

| 赛题要求 | 本方案 | 合规 |
|---------|--------|------|
| B/S 架构 | Vue3 + FastAPI + Nginx | ✅ |
| LoongArch + 麒麟高级服务器 | 生产部署 loong64 全组件 + Kylin V11 | ✅ |
| 国产数据库 | 达梦 DM8 8.1.2.128 | ✅ |
| 大模型服务 | 豆包 doubao-seed-2-0-lite-260428 (火山引擎 ARK) | ✅ |
| PC Web 可视化 | Vue3 + ECharts + AntV G6 | ✅ |
| 双角色注册与文档上传 | 个人 + 企业, DOC/PDF | ✅ |
| 文档智能解析与结构化 | 豆包模型 + OCR + JSON Schema | ✅ |
| 能力图谱构建与可视化 | Neo4j + G6 力导引 | ✅ |
| 智能双向匹配 | 召回-粗排-精排 + 多维评分 | ✅ |
| 多维度匹配加分 | 居住城市/意向城市等 | ✅ |
| 平台管理与监控后台 | Admin-Svc + 监控看板 | ✅ |

## 附录 B: 豆包 ARK API 调用速查

```bash
# 环境变量配置
export ARK_API_KEY="ark-bde00d78-dc34-4aef-9985-345ac356fafe-45c4d"
export ARK_BASE_URL="https://ark.cn-beijing.volces.com/api/v3"
export ARK_CHAT_MODEL="doubao-seed-2-0-lite-260428"
export ARK_EMBEDDING_MODEL="doubao-embedding-text-240915"

# cURL 调用示例
curl -X POST https://ark.cn-beijing.volces.com/api/v3/chat/completions \
  -H "Authorization: Bearer ark-bde00d78-dc34-4aef-9985-345ac356fafe-45c4d" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "doubao-seed-2-0-lite-260428",
    "messages": [{"role":"user","content":"你好"}]
  }'

# Python OpenAI SDK 调用
from openai import OpenAI
client = OpenAI(
    base_url="https://ark.cn-beijing.volces.com/api/v3",
    api_key="ark-bde00d78-dc34-4aef-9985-345ac356fafe-45c4d"
)
resp = client.chat.completions.create(
    model="doubao-seed-2-0-lite-260428",
    messages=[{"role":"user","content":"解析这份简历"}]
)
print(resp.choices[0].message.content)
```

## 附录 C: 参考资料

- 火山引擎方舟 ARK 平台: https://www.volcengine.com/product/ark
- 中国科学技术大学 Job-SDF 数据集: https://job-sdf.github.io/
- 银河麒麟高级服务器操作系统: https://www.kylinos.cn/
- 达梦数据库 DM8 文档: https://eco.dameng.com/document/dm/zh-cn/start
- LoongArch 架构生态: https://www.loongnix.cn/
- Element Plus: https://element-plus.org/
- AntV G6: https://g6.antv.antgroup.com/
- FastAPI: https://fastapi.tiangolo.com/

---

> **文档结束** | 智聘云图 v2.0 完整版 | 2026-07-06
